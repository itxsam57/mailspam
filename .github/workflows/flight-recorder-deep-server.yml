name: Flight Recorder Deep Server Wiring

on:
  push:
    branches:
      - design/full-product-flight-recorder-20260816

permissions:
  contents: write

jobs:
  wire:
    if: "${{ github.event.head_commit.message == 'chore: trigger deep flight recorder server wiring' }}"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: design/full-product-flight-recorder-20260816
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Apply deep server instrumentation
        run: python scripts/engineering/apply-flight-recorder-deep-server.py
      - name: Typecheck
        run: npm run typecheck
      - name: Focused runtime trace tests
        run: npx vitest run tests/unit/runtimeTraceRequestContext.test.ts tests/unit/runtimeTraceServerCoverage.test.ts tests/unit/runtimeWorkflowTraceV2.test.ts tests/unit/runtimeTraceCheckpoint.test.ts tests/unit/runtimeTraceManifest.test.ts tests/unit/workflowDiagnosis.test.ts
      - name: Commit deep server instrumentation
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No deep server source changes produced."
            exit 1
          fi
          git config user.name "email-shield-engineering"
          git config user.email "email-shield-engineering@users.noreply.github.com"
          git add server/src
          git commit -m "feat: trace provider and scan server checkpoints"
          git push origin HEAD:design/full-product-flight-recorder-20260816
