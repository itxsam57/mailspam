# Email Shield — Automated and Manual Test Matrix

This matrix is project-specific. A check is installed only when it protects an existing Email Shield architecture or workflow. The canonical desktop engineering gate now runs on Windows, macOS and Ubuntu; Linux additionally starts an isolated Secret Service user session for native vault verification.

## Automated engineering gate

| ID | Area | Check | Command | Platform | Gate behavior |
|---|---|---|---|---|---|
| A-01 | Environment | Node.js 22, required files, npm workspace and lockfile-v3 validation | `npm run preflight` | Windows + macOS + Ubuntu | Blocking |
| A-02 | Repository hygiene | Merge-marker, tracked-secret and targeted secret-pattern scan | `npm run preflight` | Windows + macOS + Ubuntu | Blocking |
| A-03 | Type safety | Strict TypeScript over production source and tests, no emit | `npm run typecheck` | Windows + macOS + Ubuntu | Blocking |
| A-04 | Production compilation | Compile `server/src` to `server/dist` | `npm run build` | Windows + macOS + Ubuntu | Blocking |
| A-05 | Unit/API behavior | Detection, MIME, provider normalization, policy, community privacy, aggregation, signatures, local API security, APIs and architecture | `npm run test:unit` | Windows + macOS + Ubuntu | Blocking |
| A-06 | Integration behavior | Entire scam corpus through all five provider fixtures | `npm run test:integration` | Windows + macOS + Ubuntu | Blocking |
| A-07 | Worker runtime | Compiled Worker startup, verified-feed input, progress and termination | `npm run test:integration` | Windows + macOS + Ubuntu | Blocking |
| A-08 | Browser source | Parse inline JavaScript and syntax-check every `web/*.js` file | `npm run check:web` | Windows + macOS + Ubuntu | Blocking |
| A-09 | Browser wiring | Required DOM, local security wrapper, dynamic scripts, Report Scam/Spam/Junk and API endpoint contracts | `npm run check:web` | Windows + macOS + Ubuntu | Blocking |
| A-10 | Browser privacy | No body, raw HTML, campaign payload, reporter proof, provider identifier or readable local-session secret in community/action UI | `npm run check:web` | Windows + macOS + Ubuntu | Blocking |
| A-11 | Server startup | Start compiled service on an isolated localhost port with loopback-only validation | `npm run smoke:server` | Windows + macOS + Ubuntu | Blocking |
| A-12 | API smoke | Homepage, accounts API, fixture connection, quick-scan SSE completion, authenticated local-session checks, community status and account removal | `npm run smoke:server` | Windows + macOS + Ubuntu | Blocking |
| A-13 | Corpus smoke | `/api/dev/test-suite` returns zero corpus/parity failures through the authenticated desktop API | `npm run smoke:server` | Windows + macOS + Ubuntu | Blocking |
| A-14 | Dependency inventory | Capture production/development advisory evidence | `npm run audit:inventory` | Ubuntu CI; local when enabled | Evidence/advisory |
| A-15 | Production dependencies | Fail on high or critical production advisories | `npm run audit:prod` | Ubuntu CI; local when enabled | Blocking |
| A-16 | Evidence | JSON/Markdown report, dependency evidence and browser handoff | `npm run gate` | Windows + macOS + Ubuntu | Always generated |
| A-17 | Provider Spam/Junk | Exact-one Gmail Spam, Outlook Junk, iCloud/Yahoo/IMAP special-use Junk behavior | `npm run test:unit` | Windows + macOS + Ubuntu | Blocking |
| A-18 | Adult campaign intent | Explicit adult-site campaign reaches High Risk without brand-specific rules | `npm run test:unit` | Windows + macOS + Ubuntu | Blocking |
| A-19 | Report privacy | Community context excludes subject/body/mailbox/provider/raw URL/attachment-name data | `npm run test:unit` | Windows + macOS + Ubuntu | Blocking |
| A-20 | Independent aggregation | Duplicate reporter, warning/confirmed thresholds, indicator support and rate limits | `npm run test:unit` | Windows + macOS + Ubuntu | Blocking |
| A-21 | Signed feed | Ed25519 signing, key-pair validation, tamper/trust/freshness/expiry rejection | `npm run test:unit` | Windows + macOS + Ubuntu | Blocking |
| A-22 | Community HTTP | Server mode disabled by default; ingestion/public key/signed feed contracts when enabled | `npm run test:unit` | Windows + macOS + Ubuntu | Blocking |
| A-23 | Local and offline protection | Immediate local campaign memory and encrypted retry outbox | `npm run test:unit` | Windows + macOS + Ubuntu | Blocking |
| A-24 | Provider parity | Gmail, iCloud, Outlook, Yahoo and Generic IMAP produce the same privacy-reduced report contract | `npm run test:unit` | Windows + macOS + Ubuntu | Blocking |
| A-25 | Local session boundary | HttpOnly process-local session, CSRF-protected reads, session expiry and absence of session material from HTML/browser storage | `npm run test:unit`, `npm run check:web`, `npm run smoke:server` | Windows + macOS + Ubuntu | Blocking |
| A-26 | Mutation replay defence | Exact same-origin proof, expiring one-time nonces and successful opaque-action replay rejection | `npm run test:unit`, `npm run smoke:server` | Windows + macOS + Ubuntu | Blocking |
| A-27 | Local network isolation | Loopback-only binding, Host allowlist, forwarded-header rejection and DNS-rebinding raw HTTP probe | `npm run test:unit`, `npm run smoke:server` | Windows + macOS + Ubuntu | Blocking |
| A-28 | Browser isolation | Per-response CSP nonce, anti-framing, same-origin opener/resource policy, no-referrer and restricted browser capabilities | `npm run test:unit`, `npm run check:web`, `npm run smoke:server` | Windows + macOS + Ubuntu | Blocking |
| A-29 | Local error privacy | Exact credentials, OAuth codes, bearer values and JWT-like values are redacted from API errors and SSE output | `npm run test:unit`, `npm run check:web` | Windows + macOS + Ubuntu | Blocking |
| A-30 | Bounded scan progress | Live IMAP Quick Scan preserves its ten-message limit while yielding smaller cumulative pages; remote community refresh cannot block worker startup; first-result and between-page stalls end visibly instead of leaving an endless EventSource | `scanProgressRuntime.test.ts`, `npm run smoke:server` | Windows + macOS + Ubuntu | Blocking |
| A-31 | Live IMAP text integrity | Truncation uses selected MIME-part limits rather than complete-message size; root single-part bodies use `TEXT`; bounded plain and HTML alternatives decode in one provider request; HTML destinations reach the canonical envelope; full raw source remains unfetched and attachment bodies are fetched only through separately bounded QR or exact-hash MIME-part inspection paths | `imapMimeParts.test.ts`, architecture regression tests | Windows + macOS + Ubuntu | Blocking |
| A-32 | Credential vault boundary | Opaque target derivation, size validation, fail-closed unavailable backends, write/read/delete contract, secret-safe error handling, and native-store factory selection | `credentialVault.test.ts` | Windows + macOS + Ubuntu | Blocking |
| A-33 | App-password session custody | Long-lived iCloud/Yahoo/generic-IMAP sessions keep raw app passwords out of persistent session config when a native vault is available, resolve vault handles only at provider connect, preserve policy identity across password rotation, reference-count shared credentials, serialize reconnect/remove lifecycle, fail account creation on native write failure, keep last session on native delete failure, and use memory-only nonpersistent handles when the expected native service is unavailable | `secureSessionCredentials.test.ts`, strict type/build, existing Worker/server regression suites | Windows + macOS + Ubuntu | Blocking |
| A-34 | Guided Gmail OAuth | Desktop Authorization Code + PKCE S256, high-entropy state/nonce, exact random-port `127.0.0.1` callback Host/path/method, callback replay rejection, bounded token responses, verified Google ID-token nonce, stable `sub` policy identity, native-vault/memory-only refresh-token custody, validation+commit serialization, final-account provider revocation, revocation-failure truthfulness and browser token/code privacy | `gmailOAuthSecurity.test.ts`, `gmailOAuthRevocation.test.ts`, `npm run check:web`, strict type/build | Windows + macOS + Ubuntu | Blocking |
| A-35 | Guided Outlook OAuth | Microsoft public desktop Authorization Code + PKCE S256, exact `offline_access`/`User.Read`/`Mail.ReadWrite` scopes, no guided client secret, random localhost callback with IPv4 loopback listener, state/replay protection, Graph `/me.id` stable identity, native-vault refresh-token custody, same-reference token rotation, account-mismatch rejection, rotation-write fail-closed behavior and browser token/code privacy | `microsoftOAuthSecurity.test.ts`, `outlookRefreshRotation.test.ts`, `npm run check:web`, strict type/build | Windows + macOS + Ubuntu | Blocking |
| A-36 | Personal-policy encryption-key custody | No new raw `personal-policy.key`; legacy-key migration authenticates the encrypted database, writes/reads back the same `local-encryption-key` through an available native vault before deleting the legacy key, fails closed on missing/conflicting/unreadable key state, and uses memory-only policy state rather than plaintext fallback when no persistent native service is available | `policyKeyVaultMigration.test.ts`, `policyKeyWindowsNative.test.ts`, `policyPersistence.test.ts`, strict type/build | Windows + macOS + Ubuntu contract; Windows real legacy migration | Blocking |
| A-37 | Cross-platform native vaults | Windows Credential Manager, macOS Keychain and Linux Secret Service use the shared opaque-reference contract; native write secrets stay out of argv and shell execution; macOS writes use SecurityTool interactive stdin; Linux writes use `secret-tool` stdin inside a real Secret Service login-session bus; each desktop backend performs a real ephemeral native write/read/delete round trip in CI | `credentialVault.test.ts`, `macosKeychainVault.test.ts`, `linuxSecretServiceVault.test.ts`, three-platform CI | Windows + macOS + Ubuntu native round trips | Blocking |
| A-38 | Personal Policy Management Centre | Selected-account search/filter over all six policy classes; protected single/bulk revoke, category clear and exact-confirmation reset; policy-only versioned export excludes account/session identifiers, vault references and every provider/OAuth credential class; strict merge/replace import rejects unknown fields, validates category values and persists atomically; management mutations invalidate stale scan actions; browser rendering uses text APIs and no browser storage | `policyManagementApi.test.ts`, `policyManagementWeb.test.ts`, `localApiSecurity.test.ts`, `npm run check:web`, strict type/build | Windows + macOS + Ubuntu | Blocking |
| A-39 | Encrypted resumable scan history | Separate native-vault-backed AES-256-GCM scan-state key; bounded selected-account history; Quick/Full/Spam provider cursor checkpoints; SHA-256-only sender/message recurrence state; startup `running`→`interrupted` recovery; stopped/failed/interrupted resume by opaque scan ID; completed checkpoint deletion; browser cursor/checkpoint exclusion; dashboard refresh keeps the Worker/checkpoint alive while detached streams stop generating browser action tokens; missing protected keys fail closed and fresh unavailable-native-vault environments remain memory-only with no plaintext fallback | `scanStatePersistence.test.ts`, `resumableScanWorkflow.test.ts`, `scanHistoryApi.test.ts`, `scanDetachedStreamContract.test.ts`, `npm run check:web`, compiled Worker/server smoke | Windows + macOS + Ubuntu | Blocking |
| A-40 | Encrypted mailbox relationship history | Separate native-vault-backed AES-256-GCM relationship-history key; per-account HMAC sender/message/Reply-To identities; bounded aggregate counts/timestamps/folder categories; duplicate-message idempotency across repeated scans and resume; conservative established-history threshold with zero trust score; canonical first-contact semantics preserved; explicit authentication downgrade/stable Reply-To change/repeated suspicious-history risk signals; relationship commit before resumable cursor advancement; Worker/server observation and HMAC material excluded from browser progress; encrypted missing-key fail-closed and fresh unavailable-native-vault memory-only behavior | `relationshipHistoryPersistence.test.ts`, `relationshipContextHistory.test.ts`, `relationshipScanWorkflow.test.ts`, `relationshipHistoryArchitecture.test.ts`, strict type/build, compiled Worker/server smoke | Windows + macOS + Ubuntu | Blocking |
| A-41 | Production QR decoding | Local/offline PNG/JPEG QR decode under strict image-count, encoded-byte, dimension, decoded-pixel and payload limits; real PNG/JPEG URL decode; non-URL and credential-bearing payload rejection; malformed/dimension-bomb safety; raw image-byte exclusion from canonical/browser/history/community state; QR provenance through existing link analysis; bounded IMAP supported-part-only acquisition; supported-image inspection failures block automatic Safe; no QR cloud call or provider permission expansion | `qrDecoder.test.ts`, `qrMimeNormalization.test.ts`, `imapQrParts.test.ts`, `qrArchitecture.test.ts`, strict type/build, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |
| A-42 | Analyze Links DNS-pinned transport | Explicit-only destination acquisition resolves once per redirect hop, rejects any mixed/non-public DNS answer, pins the HTTP(S) socket to a validated address while preserving the original Host/SNI/certificate identity, revalidates redirects, rejects URL credentials/non-HTTP(S)/compressed/oversized responses, caps 3 redirects/512 KiB/5 s, sends no mailbox auth/cookies, and never treats unsupported content as benign; automatic mailbox scan paths remain network-free | `hardenedFetchDnsPin.test.ts`, `analyzeLinksNetworkArchitecture.test.ts`, strict type/build, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |
| A-43 | Mailbox thread-continuity derivation | Provider-neutral MIME normalization extracts only bounded explicit RFC `<msg-id>` thread references, retaining the newest 20 unique IDs from a bounded header tail; relationship annotation HMAC-compares them against the selected account's existing replay index, deletes raw references before scoring/browser output, never treats unknown history or a bare `Re:` as broken continuity, promotes a proven known-thread stable Reply-To route change to the specific mid-thread signal without double-counting the generic relationship signal, and emits continuity-break evidence only for an established sender with a locally known direct parent plus an explicit contradictory References chain | `mimeHeaderNormalization.test.ts`, `relationshipContextHistory.test.ts`, `relationshipScanWorkflow.test.ts`, strict type/build, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |
| A-44 | Bounded attachment-hash intelligence | Exact hashing is provider-neutrally capped at 4 attachments and 2 MiB decoded bytes each; Gmail/Outlook/raw-MIME providers hash only eligible complete bytes already local without an extra provider request; iCloud/Yahoo/generic IMAP fetch only selected eligible attachment MIME parts with no full-message fallback, reject provider responses shorter than BODYSTRUCTURE declared, and reuse only completely acquired QR image bytes; incomplete exact-hash coverage blocks automatic Safe only when verified attachment-hash intelligence is present; community reporting may contain SHA-256 but never attachment names or bytes | `attachmentHashIntelligence.test.ts`, `imapAttachmentHash.test.ts`, `imapQrParts.test.ts`, architecture regression tests, strict type/build, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |
| A-45 | Bounded HTML interaction normalization | Provider-neutral canonical interaction parsing handles quoted/unquoted attributes, security-relevant entities, accepted HTTP(S) BASE-relative anchors/areas, form/formaction and META-refresh destinations, plus plain-text URLs beside HTML; SCRIPT/STYLE raw text stays inert; 512 KiB HTML, 512 KiB plain-text, 4096-tag and 256-destination bounds fail closed to incomplete coverage; password forms and automatic redirects use structural low/moderate evidence; signed intelligence and community reporting reuse the existing privacy-reduced link/domain paths | `htmlInteractionNormalization.test.ts`, `htmlInteractionBounds.test.ts`, `generalizedIdentityArchitecture.test.ts`, strict type/build, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |
| A-46 | Attachment type integrity | Provider-neutral attachment classification consumes the already-canonical filename/extension and declared MIME type; dangerous executable/script, macro-enabled Office and archive media types close harmless-filename rename gaps without double-counting matching extension evidence; Unicode NFKC/trailing-space normalization prevents simple extension disguise; bidi filename controls create explicit risk while evidence text strips bidi/C0 controls and is bounded; metadata-only classification must not introduce any new attachment fetch, permission, persistence or community-report field | `attachmentTypeIntegrity.test.ts`, existing attachment/IMAP normalization tests, strict type/build, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |
| A-47 | Author-domain authentication alignment | After A-56 trusted Authentication-Results provenance is established, organizational trust and bounded-content Safe authentication prerequisites require RFC5322.From alignment: trusted DMARC pass qualifies; trusted explicit DMARC fail is authoritative negative; when trusted DMARC is unavailable/none, only same-result RFC 8601 SPF `smtp.mailfrom` or DKIM `header.d` aligned to From may qualify; bare mechanism passes and cross-result identity borrowing are rejected; untrusted results are non-authoritative under A-56; no DNS/provider call, permission, persistence, browser or community expansion | `authenticationAlignmentIntegrity.test.ts`, `authenticationResultsProvenance.test.ts`, generalized identity tests, strict type/build, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |
| A-48 | PSL-backed registrable-domain boundary | All organization/domain-equivalence decisions use the locked Public Suffix List parser with ICANN + private rules; deep suffixes, wildcard/exception rules and private multi-tenant suffixes cannot collapse unrelated registrants; bare public suffixes cannot authorize trust; author-auth fallback and link-domain comparisons reuse the same helper; no runtime DNS/registry/provider lookup is introduced | `publicSuffixDomainBoundary.test.ts`, authentication/identity/link regressions, strict type/build, locked dependency install, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |
| A-49 | Bounded signed community-feed resources | Remote report receipts are capped at 32 KiB and signed feed/cache documents at 4 MiB using streamed byte accounting before JSON parsing; v1 signed feeds are schema/entry/string/fan-out bounded before detector use; Ed25519 signatures must decode canonically to exactly 64 bytes; oversized/invalid feeds become unavailable rather than clean or truncated; a still-fresh verified cache may survive a bad refresh; durable report acceptance is not reversed by later feed-capacity publication failure; no provider/mailbox permission or public-deployment claim is added | `communityFeedBounds.test.ts`, `communityFeedCapacitySemantics.test.ts`, community signing/network/API/architecture regressions, strict type/build, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |
| A-50 | Community disaster recovery and signing-rotation preparation | Portable community recovery uses a bounded AES-256-GCM envelope with reviewed scrypt derivation, strict authoritative-file manifests and descriptor-bound no-follow source reads; CLI backup secrets come only from an owner-only passphrase file and never argv; restore targets must be new paths and are staged, cryptographically/store-validated, then atomically cut over; rotation preparation generates and self-verifies a distinct next Ed25519 pair plus public overlap manifest while leaving the active signer unchanged; no CI result may claim the production restore/rotation ceremony completed | `communityRecoveryOperations.test.ts`, community signing/storage/network regressions, strict type/build, compiled operations smoke through build, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |
| A-51 | Community service readiness integrity | Public `/health` readiness is proof-based rather than a server-enabled flag: server mode plus the real aggregate-build, Ed25519 sign and self-verification path must succeed or the route returns generic HTTP 503 false booleans; successful proofs are process-cached for 15 seconds and failures for 2 seconds so unauthenticated probes cannot repeatedly rebuild/sign the feed; first-use file-managed signing-key initialization has one atomic private-key creator, contenders wait for and validate that same pair, and abandoned/invalid partial state remains fail-closed; no provider/mailbox permission or public-deployment claim is added | `communityReadinessIntegrity.test.ts`, `communitySigningInitialization.test.ts`, community API/signing/network regressions, compiled dedicated-community smoke, strict type/build, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |
| A-52 | Public community error integrity | The dedicated unauthenticated community HTTP surface uses typed internal failures plus one stable JSON/no-store error boundary: malformed JSON, invalid runtime report schema, request size, reporter rate limit, operational unavailability, unexpected middleware failure and unknown routes map to reviewed machine codes without raw exception/stack/path/crypto/storage or attacker-controlled detail; runtime report fields/indicators are strictly type/bound validated before encrypted persistence; no message-substring status classification, provider/mailbox permission or deployment/gateway claim is allowed | `communityPublicErrorIntegrity.test.ts`, community architecture/API/reporting/network regressions, compiled dedicated-community smoke, strict type/build, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |
| A-53 | Encrypted community aggregate-state integrity | AES-256-GCM authenticity is followed by strict validation of the exact nested version-1 state emitted by the writer: database/campaign/reporter shapes, canonical 64-hex campaign/reporter identities, canonical ordered timestamps, persisted score/verdict bounds, sorted unique indicator support referencing existing reporters, full canonical campaign support and evidence-code counter bounds must hold before stats/feed/report acceptance/readiness/backup consume state; structurally invalid but correctly authenticated state is preserved and fails closed; existing `MAX_CAMPAIGNS` remains the growth ceiling and no provider/mailbox/deployment expansion is added | `communityAggregateStateIntegrity.test.ts`, community reporting/recovery/readiness regressions, strict type/build, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |
| A-54 | Recoverable community aggregate storage boundary | The live encrypted aggregate database must remain inside the already-accepted portable recovery source budget: one shared 192 MiB authoritative-source ceiling reserves a 32-byte storage key plus two bounded 64 KiB signing keys and derives the aggregate-file ceiling; recovery aliases that shared source constant and configured signing PEMs obey the same key budget; aggregate reads use same-descriptor regular-file/size checks before content allocation; writes predict exact v1 envelope size before encryption and recheck serialized size before temp-file persistence so an over-bound report cannot replace the last recoverable database; no lower campaign quota, provider/mailbox permission or deployment claim is added | `communityRecoverableStorageBoundary.test.ts`, community recovery/aggregate-state/readiness/reporting regressions, strict type/build, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |
| A-55 | Community storage-key read integrity | The existing 32-byte aggregate encryption key keeps its atomic `wx` creation/`EEXIST` contender contract, but runtime loading must open the exact key path read-only with no-follow where supported, prove the opened descriptor is a regular file of exactly `COMMUNITY_STORAGE_KEY_BYTES` before reading, read from that same descriptor, reject size changes, zero the generated candidate buffer after create attempt and cache only validated key bytes; malformed/oversized key files and POSIX symlinks fail closed; no key-location/format/custody/provider/mailbox/deployment expansion is added | `communityStorageKeyReadIntegrity.test.ts`, aggregate/recovery/storage-boundary regressions, strict type/build, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |
| A-56 | Authentication-Results provenance integrity | SPF/DKIM/DMARC/ARC values are actionable only when canonical `providerTrust` is explicitly `trusted`; missing/unknown/suspicious provenance cannot grant authenticated identity, relay trust, authentication-derived Safe/message/link suppression or relationship-auth history, and cannot manufacture transport-auth failure evidence or relationship downgrade; untrusted transport authentication is incomplete rather than positive/negative fact; live raw-MIME Gmail/Outlook/IMAP-family paths remain non-authoritative until a documented provider-specific producer boundary is proven; fixture trust is explicit per message and generated-corpus consumers use one manifest provenance field; ARC/API/mailbox access/guessed authserv IDs cannot bypass the boundary | `authenticationResultsProvenance.test.ts`, authentication-alignment/identity/relationship/HTML/corpus/dev-suite regressions, strict type/build, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |
| A-57 | RFC 8058 one-click unsubscribe integrity | Automatic one-click POST requires exactly one bounded raw `List-Unsubscribe` field and one `List-Unsubscribe-Post` field, an HTTPS target plus One-Click declaration, REG-066 trusted `dkim=pass` with exact `header.d`+`header.s`, exactly one matching raw DKIM signature candidate, and `h=` coverage of both required list headers; raw multi-target List-Unsubscribe values must be preserved losslessly; duplicate/mismatched/missing DKIM or list headers, ARC-only success, untrusted provenance, signature/header ambiguity, or DKIM/header resource-limit exhaustion must fall back to manual unsubscribe and never POST; browser confirmation plus the existing public-address-pinned credential-free HTTPS POST remain mandatory | `rfc8058OneClickIntegrity.test.ts`, `rfc8058DkimResourceBounds.test.ts`, unsubscribe/MIME/auth regressions, strict type/build, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |
| A-58 | Local persistence descriptor/read/commit integrity | Local encrypted/security-sensitive files use one descriptor-bound bounded reader with no-follow where supported, regular-file proof, pre-allocation size contracts, exact-size read plus growth probe/recheck; raw private local keys require owner-only POSIX permissions; plaintext ceilings are distinct from derived encrypted Base64 envelope ceilings; first-use generated key candidates are zeroed; failed atomic replacement preserves the last good destination; resumable-scan persistence failures are explicit rather than silently swallowed | `localFileIntegrity.test.ts`, `communityStorageKeyReadIntegrity.test.ts`, `scanProgressRuntime.test.ts`, policy/scan/relationship/community persistence regressions, strict type/build, full corpus/Worker/server/browser gate | Windows + macOS + Ubuntu | Blocking |

## Final visible browser test — owner only

After a green gate, the generated handoff contains only subjective/visible checks:

1. page rendering and responsive layout;
2. all five fixture-provider scans;
3. Safe and warning-card action presentation;
4. Report Scam privacy and independent-reporting confirmation text;
5. immediate local campaign protection after a rescan;
6. optional sender block remaining separate;
7. Move to Spam/Junk remaining separate and exact-message only;
8. Stop/restart responsiveness and account isolation;
9. visible local-session behavior after refresh and process restart;
10. duplicate successful action controls requiring a rescan;
11. controlled live iCloud presentation when credentials are available;
12. live provider scans show bounded-batch status at least every 15 seconds, produce partial results as batches complete, or stop with a specific timeout error rather than remaining indefinitely on Scanning;
13. ordinary short live iCloud messages no longer uniformly show the former `Readable text was bounded to 24576 bytes` note, and HTML-only/multipart messages show available content and link evidence;
14. guided Gmail live connect, Quick Scan, Disconnect, reconnect and post-reconnect Quick Scan remain accepted and must not regress;
15. guided Outlook opens Microsoft consent as a public desktop client, returns to Email Shield without exposing code/tokens, adds the Outlook account, completes Quick Scan, Disconnect removes the local protected credential, and reconnect + Quick Scan succeeds with stable policy identity despite refresh-token replacement;
16. Personal Policy Management follows the selected account, search/filter stays readable, revoke/bulk/category/reset update counts and rows correctly, export downloads a policy-only JSON file, and merge/replace import changes only the selected account.

The local session, CSRF, nonce, Host, redaction, bounded scan deadlines, selected-part MIME decoding, warning/confirmed aggregation, cryptographic feed verification, cross-provider report contract, app-password ownership lifecycle, Gmail OAuth lifecycle, Microsoft PKCE/stable-identity/token-rotation contracts, personal-policy key migration rules, all three desktop native vault backends, Personal Policy Management data/API/privacy behavior, resumable-scan encryption/restart/cursor/privacy behavior, encrypted account-local relationship-history privacy/idempotency/non-authorizing behavior, bounded mailbox thread-reference derivation/privacy/non-guessing behavior, bounded local PNG/JPEG QR decoding/privacy/resource behavior, bounded exact attachment-hash acquisition/intelligence/privacy behavior, bounded HTML/plain-text interaction normalization/resource behavior, attachment type-integrity/provider-parity/spoof-resistant evidence behavior, author-domain authentication-alignment behavior, PSL-backed registrable-domain boundary behavior, bounded signed community-feed acquisition/validation behavior, community encrypted-recovery/signing-rotation preparation behavior, community service readiness/race-safe signing initialization behavior, dedicated public-community error isolation/runtime-report validation behavior, encrypted community aggregate nested-state integrity behavior, recoverable community aggregate storage-boundary behavior, community aggregate storage-key read-integrity behavior, Authentication-Results provenance/fail-closed trust behavior, RFC 8058 one-click DKIM/list-header authorization behavior, and Analyze Links DNS-pinning/SSRF/redirect/resource-boundary behavior are automated. Real provider authorization, mailbox acceptance, controlled real-destination acceptance and subjective browser presentation remain owner-controlled because CI receives no live mailbox credentials and does not visit arbitrary external email destinations.

## Not applicable

| Area | Reason |
|---|---|
| Relational DB migrations/RLS/seeds | Encrypted local JSON stores are used; no relational database exists. |
| Component-framework tests | UI is vanilla HTML/JavaScript. |
| Mobile-native build | No Android/iOS native project exists. |
| Docker/Kubernetes | No container definition is currently canonical. |
| Production cloud health | No public deployment target is connected to CI. |
| Visual snapshots | Owner performs final visible acceptance. |
| Real provider destructive CI | CI must never receive mailbox credentials or modify live mail. |
| Gateway/DDoS testing | Requires the actual production reverse proxy/API gateway. |

## Change-impact rule

Every future provider, OAuth flow, credential store, report indicator, threshold, key format, storage layer, external endpoint or user-visible action must update this matrix and add applicable automated protection.
