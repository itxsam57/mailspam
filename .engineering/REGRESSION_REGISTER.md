# Email Shield — Regression Register

Status values:

- **PRE-EXISTING-FIXED** — defect existed before the automation installation, was exposed by the stronger gate and corrected without deleting its history.
- **DEPENDENCY-ADVISORY** — current transitive dependency risk is inventoried; production high/critical policy remains blocking and upgrades require a separate reviewed change.
- **LOCKED** — fixed behavior with automated regression coverage; future failure blocks the gate.
- **LIVE-PASS** — verified against a controlled real mailbox and backed by automation where possible.
- **RESOLVED** — a previously registered gap is implemented and remains in the historical register.
- **KNOWN-GAP** — acknowledged incomplete product or deployment capability; not misreported as passing.
- **MANUAL** — requires final visible owner acceptance after automation passes.

## Pre-existing findings exposed during installation

| ID | Status | Finding | Resolution / protection |
|---|---|---|---|
| PRE-001 | PRE-EXISTING-FIXED | An existing test fixture omitted required `diagnostics.contentCoverage`; the former command did not typecheck test sources. | Fixture corrected; strict source-plus-test typecheck is blocking on Windows and Ubuntu. |
| DEP-001 | DEPENDENCY-ADVISORY | The August 5, 2026 locked install reported full-tree transitive advisories, while the production-only audit had no high/critical failure. | Current package-level evidence is generated by `audit:inventory`; high/critical production advisories remain blocking. No forced breaking upgrade. |

## Locked regressions

| ID | Status | Regression / required invariant | Automated protection |
|---|---|---|---|
| REG-001 | LOCKED | SSE lifecycle must not cancel a scan when the request setup completes; startup/errors remain visible. | architecture/API smoke tests |
| REG-002 | LOCKED | Compiled Worker starts on Windows and Ubuntu without TypeScript runtime loaders. | Worker runtime test, CI matrix |
| REG-003 | LOCKED | Partial messages with evidence remain Review/High Risk; unavailable content cannot silently become Safe. | verdict/workflow tests |
| REG-004 | LOCKED | IMAP scans fetch selected bounded MIME parts rather than raw messages; attachment bytes may be acquired only by the separately bounded QR and exact-hash inspection paths and never by full-message fallback. | MIME/architecture tests |
| REG-005 | LOCKED | Diagnostics expose privacy-reduced metadata only; no body, raw HTML, credential or unsubscribe target. | UI/privacy tests, `check:web` |
| REG-006 | LIVE-PASS | One-message Trash succeeds only after exact reversible provider confirmation. | cleanup/UI tests, prior live iCloud pass |
| REG-007 | LOCKED | Block sender/domain is selected-account scoped and never moves mail. | session/policy tests |
| REG-008 | LOCKED | IMAP timeouts identify the stage, retry only before progress and force-close stalled sockets. | timeout/retry tests |
| REG-009 | LIVE-PASS | Personal policies survive restart, remain account isolated and are encrypted at rest. | persistence tests, prior live restart pass |
| REG-010 | LOCKED | RFC 8058 unsubscribe uses opaque tokens, exact body, pinned public address and no redirects. | unsubscribe/security tests |
| REG-011 | LOCKED | Folder discovery, unsubscribe methods and bounded-content behavior remain provider neutral. | provider/folder tests |
| REG-012 | LOCKED | Authenticated official/private-relay mail avoids false Unknown while unrelated identities remain untrusted. | identity tests |
| REG-013 | LOCKED | Safe mail remains compact, reviewable and privacy reduced. | Safe-audit tests |
| REG-014 | LOCKED | Detection must not regress to mailbox-specific compiled brand/domain lists. | generalized identity tests |
| REG-015 | LOCKED | Mark Safe is exact-message only; Trust sender is exact-address scoped; blocks/signed confirmed threats win. | review/verdict tests |
| REG-016 | LOCKED | Gmail, iCloud, Outlook, Yahoo and generic IMAP use one canonical scan/action/community-report contract. | corpus/provider-neutral tests |
| REG-017 | LOCKED | Stop returns control without freezing the server and a new scan can start. | Worker tests; manual handoff |
| REG-018 | LOCKED | Browser scripts, DOM IDs and API endpoints remain syntactically and structurally connected. | `check:web` |
| REG-019 | LOCKED | Compiled server starts locally and completes fixture connection plus quick-scan SSE. | `smoke:server` |
| REG-020 | LOCKED | One command writes complete verification and manual-handoff evidence even after a failure. | engineering gate/CI artifacts |
| REG-021 | LOCKED | High-confidence sexual/private-photo/profile lures reach High Risk while ambiguous social introductions do not all escalate. | romance-lure tests |
| REG-022 | LOCKED | **Move to Spam/Junk** operates on exactly one opaque token, requires provider confirmation, works across Gmail/Outlook/IMAP providers and never pretends to create shared protection. | `reportSpam.test.ts`, UI/privacy tests |
| REG-023 | LOCKED | Explicit first-contact adult-site solicitation with external destination and unrelated Reply-To reaches High Risk generically. | adult campaign tests |
| REG-024 | LOCKED | **Report Scam to Email Shield** creates immediate encrypted local campaign protection and may block the exact sender only by a separate explicit choice. | community/session/policy/API tests |
| REG-025 | LOCKED | Community submissions contain only privacy-reduced indicators; they exclude mailbox identity, subject, body, contacts, credentials, provider IDs, raw URL paths/query values and attachment names/content. | community privacy tests, browser boundary gate |
| REG-026 | LOCKED | One reporter counts once; candidates are private; warnings require 3 independent weighted reports; confirmed campaigns require 5 reporters, 3 strong reports and confirmed weight; each confirmed indicator requires all 5 reporters. | aggregate/API tests |
| REG-027 | LOCKED | Generic no-reply/reporting shared carriers are not globally published as exact malicious senders; campaign/reply/destination indicators provide protection instead. | fingerprint/aggregate tests |
| REG-028 | LOCKED | Community feeds are canonical Ed25519-signed, trusted, fresh and bounded; tampered, untrusted or expired feeds become unavailable, never clean. | signing/network/global-intelligence tests |
| REG-029 | LOCKED | Failed remote reports enter an encrypted bounded outbox and retry later without losing immediate local protection. | network/outbox tests |
| REG-030 | LOCKED | Central ingestion/feed/public-key endpoints are disabled by default and require explicit server mode. | community HTTP/architecture tests |
| REG-031 | LOCKED | Scan workers receive only verified feed entries and enforce warning/confirmed campaign, sender, Reply-To domain, destination domain and attachment-hash matches. | Worker/global-intelligence tests |
| REG-032 | LOCKED | Desktop mailbox and developer APIs require a process-local HttpOnly session; protected reads require CSRF proof; mutations require exact same-origin proof and an expiring single-use server nonce. | `localApiSecurity.test.ts`, compiled desktop smoke, browser gate |
| REG-033 | LOCKED | Desktop server binds only to loopback, rejects forwarded and DNS-rebinding Host requests, rate-limits sensitive routes, and invalidates successful opaque actions against replay. | local API tests, raw-HTTP smoke, browser gate |
| REG-034 | LOCKED | Local errors and SSE output redact credentials, OAuth codes, bearer values and JWT-like tokens; browser storage never holds the local session secret; CSP and anti-framing headers remain enabled. | local redaction tests, `check:web`, compiled smoke |
| REG-035 | LOCKED | A live mailbox scan must not wait for remote community retries or withhold all progress behind a large sequential IMAP page. Live IMAP scans use small bounded pages, Quick Scan keeps its ten-message limit across incremental yields, the SSE sends periodic waiting status, and first-result or between-page stalls terminate visibly instead of remaining active indefinitely. | `scanProgressRuntime.test.ts`, Worker integration, compiled desktop smoke, Windows/Ubuntu gate |
| REG-036 | LOCKED | Live iCloud/Yahoo/IMAP scans must classify readable-body completeness from the selected MIME parts, never the total RFC822 message size. Bounded plain and HTML alternatives are fetched together, decoded by their transfer encoding and charset, HTML destinations reach the canonical envelope, root single-part bodies use `TEXT`, full raw source remains unfetched, and attachment bytes are fetched only through separately bounded QR or exact-hash MIME-part inspection. | `imapMimeParts.test.ts`, architecture regression tests, Windows/Ubuntu gate |
| REG-037 | LOCKED | Every provider fixture must expose Inbox, Spam/Junk and Trash even when a special-use folder is empty; malicious fixture placement must not accidentally put every message in Inbox. | `fixtureMailboxState.test.ts`, all-five-provider fixture gate |
| REG-038 | LOCKED | An exact fixture Trash or Spam/Junk move must remain moved when a later action or scan recreates the provider adapter in the same account session. | `fixtureMailboxState.test.ts`, provider action tests |
| REG-039 | LOCKED | A stale browser tab after a desktop-process restart must validate its protected session before opening EventSource and fail immediately with a reload/reconnect message rather than remain on `Starting scan`. | browser source/architecture checks; manual stale-tab retest |
| REG-040 | LOCKED | Personal sender/domain blocks must be visibly reversible through protected account-scoped mutations, and rescanned cards must disclose existing block and locally reported-campaign state instead of silently greying controls or re-offering conflicting Safe/Trust decisions. | `policyUndoApi.test.ts`, architecture/browser checks |
| REG-041 | LOCKED | Credential persistence must fail closed when a native vault is unavailable; Windows vault targets must be opaque, secrets must never be truncated or placed in command-line arguments, and public vault failures must not retain secret-bearing lower-layer error text. | `credentialVault.test.ts`, Windows native round trip, source review |
| REG-042 | LOCKED | Windows iCloud/Yahoo/generic-IMAP account sessions must store vault handles rather than raw app passwords after provider validation; credential references must remain stable across password rotation without changing personal-policy identity; shared same-account sessions must reference-count and serialize reconnect/remove ownership; native write failure must not downgrade to insecure persistence; last-reference native deletion failure must keep the account retryable; unsupported native-store platforms may use current-process memory-only handles but no plaintext persistence substitute. | `secureSessionCredentials.test.ts`, strict type/build, Worker/server regressions, Windows/Ubuntu gate |
| REG-043 | LOCKED | Guided Gmail OAuth must use Authorization Code + PKCE S256 through an exact random-port `127.0.0.1` loopback callback; state/nonce/code must be one-time and replay resistant; ID-token identity must use verified Google `sub`; refresh-token rotation must not change personal-policy/vault identity; tokens/codes/verifier must stay outside browser responses; provider validation and credential commit must serialize against final-account revocation; final Gmail disconnect must revoke provider access before local credential deletion and remain retryable if revocation cannot be confirmed. | `gmailOAuthSecurity.test.ts`, `gmailOAuthRevocation.test.ts`, browser/privacy checks, strict type/build, Windows/Ubuntu gate |
| REG-044 | LOCKED | Every explicit guided **Connect Gmail** flow must request a fresh offline refresh token with `access_type=offline` plus `prompt=consent`; a prior Google consent left behind by a failed local setup must not cause a later connection to fail because Google omits `refresh_token`. PKCE S256 remains mandatory and the matching application client credential must remain outside the authorization URL/browser surfaces. | `gmailOAuthGoogleCompatibility.test.ts`, strict type/build, Windows/Ubuntu gate |
| REG-045 | LOCKED | Guided Outlook must be a Microsoft public desktop client using Authorization Code + PKCE S256 with `offline_access`, `User.Read` and `Mail.ReadWrite` only; browser/token authorization must not depend on a client secret; stable identity must use Graph `/me.id`, not email or refresh token; Windows refresh-token rotation must overwrite the same opaque vault reference before the replacement is accepted; account mismatch or vault-write failure must fail closed; ordinary Disconnect must remove Email Shield's local protected credential without invoking broad Microsoft `revokeSignInSessions`. | `microsoftOAuthSecurity.test.ts`, `outlookRefreshRotation.test.ts`, `check:web`, strict type/build, Windows/Ubuntu gate |
| REG-046 | LOCKED | Personal-policy AES-256-GCM key custody must never create a new raw `personal-policy.key`; Windows startup must migrate a valid legacy 32-byte key to the `local-encryption-key` Credential Manager reference only after encrypted-database authentication plus vault write/read-back verification; conflicting or missing keys and vault failures must fail closed without resetting policy state; fresh unsupported platforms remain memory-only while existing legacy encrypted-policy data stays readable until native backends land. | `policyKeyVaultMigration.test.ts`, `policyKeyWindowsNative.test.ts`, `policyPersistence.test.ts`, strict type/build, Windows/Ubuntu gate |
| REG-047 | LOCKED | Desktop native credential custody must use the same opaque-reference vault contract across Windows Credential Manager, macOS Keychain and Linux Secret Service. Native write secrets must stay out of process argv and shell execution; macOS Keychain writes must use SecurityTool interactive stdin and survive real read-back/delete; Linux `secret-tool` writes must use stdin and survive a real Secret Service user-session round trip; a missing native service must never create a plaintext fallback. App passwords, guided OAuth refresh tokens and the personal-policy encryption key inherit native custody through the same factory. | `credentialVault.test.ts`, `macosKeychainVault.test.ts`, `linuxSecretServiceVault.test.ts`, `policyKeyVaultMigration.test.ts`, Windows/macOS/Ubuntu native CI gate |
| REG-048 | LOCKED | The Personal Policy Management Centre must remain selected-account scoped and must manage all six policy classes without bypassing encrypted persistence. Reads stay behind the protected local session; every revoke, bulk revoke, category clear, import and reset requires the same-origin/CSRF/single-use-nonce mutation boundary. Export is a strict versioned policy-only document containing no account/session key, vault reference, credential, password, OAuth token or client secret. Import accepts only that exact schema, supports explicit merge/replace, validates every category value, persists atomically with rollback on failure and invalidates stale scan-action tokens. Browser policy values render through DOM text APIs and are never persisted to localStorage/sessionStorage. | `policyManagementApi.test.ts`, `policyManagementWeb.test.ts`, `localApiSecurity.test.ts`, `check:web`, strict type/build, Windows/macOS/Ubuntu gate |
| REG-049 | LOCKED | Scan continuation and history must remain account-scoped, encrypted and privacy reduced. Quick, Full and Spam/Junk scans checkpoint only opaque provider cursors, counters, completed-folder state and SHA-256 recurrence/dedupe hashes under a separate native-vault-backed AES-256-GCM key; no raw sender/message identity, body, HTML, credential, OAuth material, unsubscribe target or browser action token may be persisted. The browser must never receive cursor/checkpoint fields. A page refresh must not cancel the local Worker; detached streams must keep checkpointing but stop minting browser-only action tokens. Startup converts stale `running` records to `interrupted`; failed/stopped/interrupted scans resume only through the selected account's opaque scan ID; completed records permanently drop their checkpoint. Missing protected keys fail closed instead of resetting encrypted history. | `scanStatePersistence.test.ts`, `resumableScanWorkflow.test.ts`, `scanHistoryApi.test.ts`, `scanDetachedStreamContract.test.ts`, `check:web`, compiled Worker/server smoke, Windows/macOS/Ubuntu gate |
| REG-050 | LOCKED | Durable mailbox relationship history must remain local, encrypted, account-isolated and non-authorizing. Persisted identities are per-account HMAC fingerprints only; raw sender/recipient addresses, contacts, subjects, bodies, HTML, URLs, Message-IDs/provider IDs, credentials, OAuth material, unsubscribe targets and action tokens are forbidden. Duplicate message fingerprints count once across repeated scans/resume. Established history requires multiple benign authenticated observations, contributes zero trust score, never marks mail Safe, never becomes an allowlist and never sets canonical `isFirstContact` false. Explicit authentication downgrade, stable Reply-To change and predominantly suspicious prior history may add risk. Relationship observations must persist before a resumable cursor advances and must be removed from browser progress. Missing protected keys fail closed; fresh unsupported platforms remain memory-only. | `relationshipHistoryPersistence.test.ts`, `relationshipContextHistory.test.ts`, `relationshipScanWorkflow.test.ts`, `relationshipHistoryArchitecture.test.ts`, strict type/build, compiled Worker/server smoke, Windows/macOS/Ubuntu gate |
| REG-051 | LOCKED | Production QR inspection must remain local, bounded and non-exfiltrating. Supported PNG/JPEG images are decoded under strict image-count, encoded-byte, dimension, pixel and payload limits; malformed/dimension-bomb input must not crash or allocate unbounded pixels. Only HTTP(S) QR destinations without embedded credentials become canonical links, tagged `source: qr`; raw image bytes and non-URL QR payloads must never enter canonical/browser/history/community state. Gmail/Outlook/fixture MIME normalization may inspect already-local supported image attachments; iCloud/Yahoo/generic IMAP may fetch only selected bounded PNG/JPEG MIME parts and must not download PDFs, archives, executables, unsupported image bodies or the full RFC822 source for QR analysis. A supported image that cannot be inspected safely blocks an automatic Safe verdict. No QR cloud service or new provider permission is allowed. | `qrDecoder.test.ts`, `qrMimeNormalization.test.ts`, `imapQrParts.test.ts`, `qrArchitecture.test.ts`, strict type/build, full corpus/Worker/server/browser gate |
| REG-052 | LOCKED | Explicit Analyze Links network acquisition must resist SSRF and DNS rebinding by resolving once per redirect hop, rejecting the hop if any DNS answer is non-public, and connecting the HTTP(S) socket directly to a validated pinned address while retaining the original hostname for Host/SNI/certificate verification. Every redirect repeats resolve/validate/pin; URL userinfo/non-HTTP(S), compressed or oversized bodies and deadline/redirect overrun fail closed; unsupported content is not downloaded or labeled benign; no mailbox cookies/auth are sent; and Quick/Full/Spam scan paths must remain free of deep-link network access. | `hardenedFetchDnsPin.test.ts`, `analyzeLinksNetworkArchitecture.test.ts`, strict type/build, full corpus/Worker/server/browser gate |
| REG-053 | LOCKED | Mailbox thread-continuity evidence must come only from bounded explicit RFC threading identifiers and account-local HMAC history. Normalization retains only the newest 20 unique `<msg-id>` values from a bounded 32 KiB header tail; raw `In-Reply-To`/`References` values are consumed and deleted before scoring, persistence or browser output even when relationship history is unavailable. Unknown references and bare `Re:` subjects never imply broken continuity. A stable Reply-To route change in a locally known thread emits the specific `REPLY_TO_CHANGED_MID_THREAD` signal without duplicate generic relationship evidence. `THREAD_CONTINUITY_BROKEN` requires an established sender, a locally known direct parent and an explicit contradictory References chain. | `mimeHeaderNormalization.test.ts`, `relationshipContextHistory.test.ts`, `relationshipScanWorkflow.test.ts`, strict type/build, full corpus/Worker/server/browser gate |
| REG-054 | LOCKED | Attachment-hash intelligence must use complete decoded bytes only and is provider-neutrally capped at 4 attachments and 2 MiB decoded bytes each. Gmail/Outlook/raw-MIME paths hash only eligible bytes already present locally; iCloud/Yahoo/generic IMAP may fetch only selected eligible MIME parts with no full-message fallback. IMAP responses shorter than BODYSTRUCTURE declared, parts that hit the encoded cap, oversized/excess-count/missing/undecodable parts remain unhashed; QR bytes are reused only after the same complete-part guard. If verified signed attachment-hash rules are present and coverage is incomplete, Global Intelligence blocks automatic Safe without manufacturing positive threat evidence. Only SHA-256 may enter the community indicator contract; attachment names/content remain excluded. | `attachmentHashIntelligence.test.ts`, `imapAttachmentHash.test.ts`, `imapQrParts.test.ts`, architecture regression tests, strict type/build, full corpus/Worker/server/browser gate |
| REG-055 | LOCKED | HTML interaction evidence must come from the bounded provider-neutral canonical parser, not quoted-anchor regexes or executed content. Quoted/unquoted anchor/area, accepted BASE-relative, form/formaction, META-refresh and companion plain-text destinations enter the existing link/intelligence path; SCRIPT/STYLE raw text stays inert; 512 KiB HTML, 512 KiB plain-text, 4096-tag and 256-destination bounds mark incomplete coverage instead of silently dropping an uninspected tail. Password forms and automatic redirects remain low/moderate structural evidence only. No remote resource load, form submission, redirect follow, compiled brand map, new provider permission or raw URL path/query community disclosure is allowed. | `htmlInteractionNormalization.test.ts`, `htmlInteractionBounds.test.ts`, `generalizedIdentityArchitecture.test.ts`, strict type/build, full corpus/Worker/server/browser gate |
| REG-056 | LOCKED | Attachment-risk classification must consume the provider-neutral canonical MIME type as well as a normalized filename view. A harmless or missing extension cannot erase declared executable/script, macro-enabled Office or archive risk; matching filename and MIME evidence for the same category must not double-count; Unicode compatibility/trailing-space disguises are normalized locally; bidi filename controls add explicit risk and are stripped from bounded evidence-display text. This metadata-only layer must not expand attachment fetching, provider permissions, persistence or community reporting. | `attachmentTypeIntegrity.test.ts`, existing attachment/IMAP normalization tests, strict type/build, full corpus/Worker/server/browser gate |
| REG-057 | LOCKED | Author-domain trust must require RFC5322.From alignment, not a bare SPF/DKIM mechanism pass. DMARC pass is sufficient; explicit DMARC fail is authoritative negative; only when DMARC is unavailable/none may same-result RFC 8601 SPF `smtp.mailfrom` or DKIM `header.d` alignment establish author authentication. Passing results must not borrow identities from failed/different segments. This local metadata-only decision must not add DNS/provider calls, persistence, browser exposure or community fields. | `authenticationAlignmentIntegrity.test.ts`, generalized identity tests, strict type/build, full corpus/Worker/server/browser gate |
| REG-058 | LOCKED | Registrable-domain trust must use the locked Public Suffix List boundary rather than a handwritten last-N-label heuristic. Deep public suffixes, wildcard/exception rules and private multi-tenant suffixes must isolate unrelated registrants; a bare known public suffix must never become an organization; SPF/DKIM fallback alignment, authenticated identity and link-domain comparisons must reuse this one helper. Runtime DNS/registry/provider lookups, duplicate per-layer suffix tables and silent dependency-driven trust-boundary changes are forbidden. | `publicSuffixDomainBoundary.test.ts`, authentication/identity/link regressions, strict type/build, locked install, full corpus/Worker/server/browser gate |
| REG-059 | LOCKED | Community network acquisition and signed-feed consumption must remain resource bounded before detector use. Remote receipts are limited to 32 KiB and feed/cache documents to 4 MiB with both declared-length and streamed-byte enforcement; version-1 feed entries, values, rule IDs and identity alias/domain fan-out are bounded and unknown fields rejected; Ed25519 signatures must be canonical 64-byte values; oversized/invalid feeds are unavailable, never clean or silently truncated; only an independently still-valid cached feed may be retained; a durably accepted report must not be falsely rejected/requeued solely because publication subsequently reaches the feed resource ceiling. | `communityFeedBounds.test.ts`, `communityFeedCapacitySemantics.test.ts`, community signing/network/API/architecture regressions, strict type/build, full corpus/Worker/server/browser gate |
| REG-060 | LOCKED | Community disaster recovery and signing-key rotation preparation must remain secret-safe, bounded and non-destructive. Portable backups use the reviewed AES-256-GCM+scrypt envelope, contain only authoritative aggregate/signing state, enforce strict known-file/hash/resource rules and never accept passphrases in argv; POSIX passphrase and sensitive source reads use same-descriptor no-follow checks with owner-only passphrase permissions. Restore must target a new path, validate the real signer/store in staging and atomically rename only after success. Rotation preparation must create a distinct self-verifying next Ed25519 pair/public overlap manifest while leaving the active signer unchanged and never placing private material in the public manifest or CLI JSON. Production restore and key-switchover ceremonies remain live GAP-004 acceptance. | `communityRecoveryOperations.test.ts`, strict type/build, full Windows/macOS/Ubuntu engineering gate |
| REG-061 | LOCKED | Public community readiness must prove the current aggregate -> sign -> verify path rather than mirror a configuration flag. Disabled, unreadable, un-signable or unverifiable state returns generic HTTP 503 with only service/readiness booleans and `Cache-Control: no-store`; internal paths, key material and diagnostics stay private. Healthy proofs are cached for 15 seconds and failures for 2 seconds so public probes cannot force full feed work per request. Concurrent file-managed first-use signing initialization must converge on the one atomically established private-key pair; contenders never overwrite it, while abandoned or invalid partial state remains fail-closed. Production load-balancer/monitor wiring remains GAP-004. | `communityReadinessIntegrity.test.ts`, `communitySigningInitialization.test.ts`, compiled community smoke, strict type/build, full Windows/macOS/Ubuntu engineering gate |
| REG-062 | LOCKED | The dedicated unauthenticated community service must never expose raw internal errors or infer public status from message text. Validation/rate-limit/capacity/disabled failures are typed internally and translated at the HTTP edge to stable JSON codes; malformed/oversized bodies are captured by the final Express error barrier; every error is `Cache-Control: no-store`; paths, stack traces, crypto/storage diagnostics and attacker-controlled values remain private. Version-1 reports require strict runtime proof/timestamp/verdict/finite-score/evidence-code/indicator validation before aggregate persistence; malformed fields are rejected rather than silently skipped. Desktop-local API error governance remains separate. | `communityPublicErrorIntegrity.test.ts`, `communityArchitecture.test.ts`, community reporting/API/network regressions, strict type/build, full Windows/macOS/Ubuntu engineering gate |
| REG-063 | LOCKED | Authenticated community aggregate ciphertext must not be trusted as valid application state solely because AES-256-GCM verification succeeds. After decryption, the version-1 database must match the exact nested writer invariants: known fields only, existing campaign ceiling, canonical campaign/reporter proofs and ISO interval timestamps, integer persisted scores/supported verdicts, valid normalized indicator keys with sorted unique support drawn only from campaign reporters, complete canonical campaign support, and valid evidence-code counts. Any structurally invalid but cryptographically authentic database is preserved and blocks stats, feed generation, subsequent report acceptance, readiness and recovery backup instead of being normalized or partly consumed. | `communityAggregateStateIntegrity.test.ts`, community reporting/recovery/readiness regressions, strict type/build, full Windows/macOS/Ubuntu engineering gate |
| REG-064 | LOCKED | Live community aggregate persistence must never outgrow the built-in portable-recovery contract. The 192 MiB authoritative-source ceiling and 64 KiB signing-key ceiling are shared runtime/recovery constants; the aggregate-file ceiling reserves the exact 32-byte storage key plus two signing-key budgets. Aggregate database reads must reject an oversized/non-regular opened file before content allocation, with same-descriptor reading and no-follow where supported. Writes must reject a predicted over-bound encrypted v1 envelope before encryption/temp-file persistence and recheck the final serialized envelope before atomic replacement. Configured signing PEMs must obey the same recovery key budget. Existing `MAX_CAMPAIGNS` and aggregation semantics remain unchanged. | `communityRecoverableStorageBoundary.test.ts`, recovery/aggregate-state/readiness/reporting regressions, strict type/build, full Windows/macOS/Ubuntu engineering gate |
| REG-065 | LOCKED | Community aggregate encryption-key loading must not read arbitrary path content before validating the existing exact 32-byte key contract. Atomic `wx` creation and `EEXIST` contender behavior remain; generated candidate bytes are zeroed after the create attempt; runtime opens the key read-only with no-follow where supported, fstats that same descriptor as a regular file of exactly `COMMUNITY_STORAGE_KEY_BYTES`, reads from the same descriptor, rechecks read length, and caches only validated bytes. Wrong-size/oversized key files and POSIX symlinks fail closed without regeneration or normalization. Key location/format/custody and provider/mailbox/deployment boundaries remain unchanged. | `communityStorageKeyReadIntegrity.test.ts`, aggregate/recovery/storage-boundary regressions, strict type/build, full Windows/macOS/Ubuntu engineering gate |

## Resolved registered gaps

| ID | Status | Former gap | Resolution |
|---|---|---|---|
| GAP-003 | RESOLVED | OS-keychain-backed local encryption keys and provider-token custody | Implemented one fail-closed native-vault abstraction across Windows Credential Manager, macOS Keychain and Linux Secret Service. App-password sessions, guided Gmail/Outlook refresh tokens and the personal-policy AES key use stable opaque references through that abstraction when the native service is available. Real native write/read/delete is exercised on all three desktop CI platforms; legacy policy-key migration authenticates existing encrypted state before verified vault commit/deletion. Missing native services remain memory-only/fail-closed with no plaintext substitute. This resolution does not claim universal hardware/application binding, uninstall cleanup, packaging acceptance or real-Outlook owner acceptance. Policy-only backup/export secret exclusion is now covered by REG-048. |
| GAP-006 | RESOLVED | Production QR decoder | Added a bounded local/offline production decoder for PNG/JPEG QR images. Decoded HTTP(S) destinations enter the canonical link pipeline with QR provenance while raw image bytes remain transient and never enter browser, history, policy or community state. Gmail/Outlook/fixture MIME uses locally parsed supported images; iCloud/Yahoo/generic IMAP fetch only selected bounded supported image parts. Size/dimension/pixel/count/payload limits, malformed-input handling and Safe-blocking incomplete inspection are regression locked. This resolution explicitly covers PNG/JPEG and does not claim universal image-format decoding. |
| GAP-007 | RESOLVED | Desktop local API authentication and CSRF protection | Implemented process-local HttpOnly sessions, CSRF-protected reads, same-origin one-time mutation authorization, Host/forwarded-header rejection, sensitive-action replay prevention, request limits, route limits, redaction and restrictive browser headers. The dashboard must still remain local-only; this does not replace future signed-executable binding work. |
| GAP-009 | RESOLVED | Complete editable policy-management centre | Added selected-account searchable management for blocked senders/domains, trusted senders, Safe exceptions, unsubscribe history and locally reported campaigns; protected single/bulk revoke, category clear and explicit full reset; strict versioned policy-only export; strict merge/replace import with validation and atomic rollback; stale scan-action invalidation; and browser privacy checks. |
| GAP-010 | RESOLVED | Persisted resumable scan cursors and scan-history presentation across restart/rate limits | Added a separate encrypted scan-state repository keyed by stable account policy identity, native-vault-backed key custody, privacy-reduced bounded history, selected-account opaque scan IDs, provider-neutral Quick/Full/Spam checkpoints, restart recovery of stale running scans, protected resume streams, background continuation across dashboard refresh, explicit Stop checkpoints, and fail-closed missing-key behavior. Provider cursor/checkpoint material remains server-side and completed records discard it. |
| GAP-011 | RESOLVED | Full mailbox-derived relationship history | Added a separate native-vault-backed AES-256-GCM relationship-history repository keyed by stable account policy identity. Sender/message/Reply-To identities are stored only as per-account HMAC fingerprints with bounded aggregate counts/timestamps/folder categories; repeated scans are idempotent by message fingerprint. The existing Relationship Context layer now receives durable local context for explicit authentication downgrade, stable Reply-To change and repeated suspicious history while conservative established history remains informational only. History never disables canonical first-contact semantics, never marks mail Safe and never overrides current personal/global/current-message threat evidence. Worker/server observations persist before resumable checkpoint advancement and are stripped before browser SSE output. |
| GAP-012 | RESOLVED | Mailbox-derived thread-continuity detection path | The detector already contained `THREAD_CONTINUITY_BROKEN` and `REPLY_TO_CHANGED_MID_THREAD` scoring rules, but production normalization never supplied either signal. Added one provider-neutral bounded `In-Reply-To`/`References` observation path at canonical MIME normalization and reused the existing per-account HMAC message replay index to prove local thread history without a new plaintext database or storage migration. Raw thread identifiers are deleted before scoring/browser output; unknown history remains non-suspicious; known-thread stable Reply-To changes and explicit known-parent/reference contradictions now activate the existing structural signals conservatively. |
| GAP-013 | RESOLVED | Attachment-hash threat-intelligence observation path | Signed/community `attachment_hash` indicators already existed but canonical attachment hashes were always null. Exact hashing is now provider-neutrally bounded to the first 4 attachments at no more than 2 MiB decoded bytes each. Gmail/Outlook/raw-MIME providers hash eligible complete bytes already available locally with no extra request; live IMAP acquires only selected eligible MIME parts, rejects short/truncated responses, reuses only completely acquired QR bytes and never falls back to the full message. Missing exact-hash coverage is explicit and blocks automatic Safe only when verified attachment-hash rules are relevant. Attachment bytes remain transient; only SHA-256 is eligible for the existing privacy-reduced community indicator contract. |
| GAP-014 | RESOLVED | HTML interactive-destination observation path | Canonical HTML destination discovery was limited to quoted anchors, so valid unquoted/entity-obfuscated destinations, accepted BASE-relative navigation, form/formaction submissions, META refresh redirects and companion plain-text URLs could bypass every downstream link/intelligence layer. Added one bounded provider-neutral non-executing interaction normalizer at MIME canonicalization, routed its destinations through existing Link Structure, signed Global Intelligence and domain-reduced community reporting, activated only low/moderate password-form/redirect structural evidence, and made HTML/plain-text/tag/destination limit exhaustion explicitly incomplete so an uninspected tail cannot become automatically Safe. |
| GAP-015 | RESOLVED | Attachment MIME-type integrity observation path | Canonical attachments already contained the provider-declared MIME type, but the attachment-risk layer ignored it and trusted filename extension alone. Activated that existing provider-neutral field for executable/script, macro-enabled Office and archive risk when filenames are misleading, avoided same-category double-counting, normalized compatibility/trailing-space filename variants, added bidi-disguise evidence with spoof-resistant bounded evidence display, and introduced no new attachment fetch, provider permission, persistence path or community schema. |
| GAP-016 | RESOLVED | Author-domain authentication alignment path | The shared `authenticationPassed` helper formerly treated any SPF or DKIM pass as authenticating RFC5322.From, allowing unrelated mechanism identities to create organizational trust and satisfy bounded-content Safe prerequisites. Replaced that inference with DMARC-aware author alignment: DMARC pass is accepted, DMARC fail is authoritative negative, and DMARC-unavailable/none fallback requires same-result RFC 8601 `smtp.mailfrom`/`header.d` organizational alignment. No DNS/network/provider-permission/persistence/community expansion was introduced. Authentication-Results `authserv-id` provenance remains a separate future trust-boundary concern, not falsely closed here. |
| GAP-017 | RESOLVED | Registrable-domain/public-suffix trust boundary | The shared organization helper previously used a last-two-label rule plus a short manual suffix list, allowing unlisted deep, wildcard or private suffixes to collapse unrelated registrants into one trust domain. Replaced that heuristic with a locked local `tldts` Public Suffix List parser using ICANN + private rules and routed all existing consumers through the same helper. Added deep-suffix, wildcard/exception, private-tenant, authentication-alignment and link-deception regressions. No runtime DNS/provider call, permission, persistence or community-schema expansion was introduced. |
| GAP-018 | RESOLVED | Bounded community-feed acquisition and signed-document validation | The remote client formerly parsed community JSON without a response-byte ceiling and verified signed feeds without a bounded v1 entry schema, so a compromised/misconfigured endpoint or pathological signed document could consume excessive memory/CPU before or after signature validation. Added streamed 32 KiB receipt / 4 MiB feed limits, bounded cache reads, strict signed-entry/fan-out/signature validation, signer-side document limits, verified-cache failover, and capacity-safe report acceptance semantics. GAP-004 public deployment/ops and GAP-008 gateway reputation/volumetric defence remain open and are not claimed by this resolution. |
| GAP-019 | RESOLVED | Community disaster-recovery and signing-rotation preparation tooling | The repository had no operator-grade way to produce an encrypted portable backup of authoritative aggregate/signing state, validate a restore before cutover, or prepare a controlled two-key Ed25519 overlap package. Added bounded AES-256-GCM+scrypt recovery bundles, strict authenticated manifests, descriptor-bound secret/source reads, atomic new-path restore after real signer/store validation, a passphrase-file-only CLI, and self-verifying next-key/public-overlap rotation packages that do not replace the active signer. This resolves missing tooling only: GAP-004 remains open until a deployed backup/restore drill and real key overlap/switchover/retirement ceremony are executed. |
| GAP-020 | RESOLVED | Community service readiness observation path | The public `/health` endpoint formerly claimed readiness from `serverEnabled` alone, so a process with unreadable aggregate state or unusable signed-feed publication could still advertise itself healthy. Readiness now uses the existing aggregate-build/sign/self-verify path, fails closed with generic HTTP 503, and uses short bounded process caching to avoid a probe-amplification DoS. The first full gate exposed and root-fixed a separate concurrent first-use signing-key `EEXIST` race: one atomic initializer now wins while contenders wait for and validate that exact pair; abandoned/invalid partial state remains fail-closed. GAP-004 remains open until real reverse-proxy/load-balancer monitoring wiring is deployed and accepted. |
| GAP-021 | RESOLVED | Unauthenticated community error-isolation and runtime-report-validation path | The dedicated public service formerly echoed internal exception messages on report/feed paths, could fall into Express default diagnostics on other routes/body errors, and classified report failures by message substrings; its aggregate validator also trusted compile-time report shapes at runtime. Added typed domain failures, strict pre-persistence version-1 runtime validation, stable JSON/no-store public error codes, route-level operational isolation and a final Express/body-parser error barrier. Real corrupt encrypted state is regression-tested across status/feed/public-key/report without leaking content, paths or diagnostics. GAP-004 deployment operations and GAP-008 gateway/volumetric controls remain open. |
| GAP-022 | RESOLVED | Decrypted community aggregate nested-state validation path | The encrypted aggregate store previously treated successful AES-GCM decryption plus a top-level version/campaigns check as sufficient and then trusted nested campaign/reporter/indicator/evidence JSON as TypeScript. Added a strict post-decryption validator for the exact state emitted by the current writer and wired every store read through it. Regression tests re-encrypt deliberately malformed nested plaintext with the legitimate local key/tag and prove stats/feed/report acceptance and disaster-recovery backup all fail closed. This does not replace GAP-004 deployed restore acceptance or change the existing campaign-growth ceiling. |
| GAP-023 | RESOLVED | Runtime aggregate/recovery size-contract mismatch | Disaster-recovery export already refused authoritative source sets above 192 MiB while the live aggregate database had no corresponding read/write ceiling, so production could grow into an officially un-backupable state and oversized files were read before size validation. Moved the recovery/source and signing-key budgets into shared resource limits, derived the live aggregate database ceiling from that same source budget, added same-descriptor pre-allocation aggregate reads, pre-encryption/final-serialized write checks, and configured-key size parity. GAP-004 remains open until a real deployed backup/restore drill proves actual production storage and cutover. |
| GAP-024 | RESOLVED | Aggregate storage-key pre-allocation/read-integrity path | The community aggregate store already required a 32-byte encryption key but loaded the path with `readFileSync` before checking length and followed POSIX symlinks. Runtime key loading now preserves atomic first-use creation while using same-descriptor regular-file/exact-size validation before key-byte reading, no-follow where supported, defensive read-length validation and generated-candidate zeroing. This is local file-read integrity only and does not change key custody, format or GAP-004 deployment acceptance. |

## Known gaps — not accepted as complete

| ID | Status | Gap | Current boundary |
|---|---|---|---|
| GAP-001 | KNOWN-GAP | Production Gmail OAuth publication/consent verification | Guided Gmail desktop OAuth and native-vault refresh-token custody are implemented; live connection, Quick Scan, Disconnect, reconnect and post-reconnect Quick Scan are owner-accepted on Windows. Public production Google consent/restricted-scope verification remains deployment work. |
| GAP-002 | KNOWN-GAP | Guided Outlook OAuth onboarding | Public-client PKCE browser onboarding, Graph stable identity and native-vault refresh-token custody/rotation are automated; controlled real-Microsoft/real-Outlook owner acceptance remains before this gap can close. |
| GAP-004 | KNOWN-GAP | Public production community deployment and operations | Self-hostable ingestion, aggregation, signing, verification and client protocol exist. DNS/TLS, gateway abuse controls, monitoring, backups and an executed key-rotation ceremony require the deployment environment. |
| GAP-005 | KNOWN-GAP | Controlled real-destination Analyze Links validation | Production resolve/validate/socket-pin transport is implemented and regression locked: every hop rejects mixed/non-public DNS, pins the socket to a validated public address, revalidates redirects, bounds time/body/redirects, sends no mailbox auth/cookies, and fails uninspectable content closed. Controlled live validation against deliberately managed public destination/redirect infrastructure still remains before this gap can close. |
| GAP-008 | KNOWN-GAP | Operational reporter reputation and gateway-level volumetric abuse defence | Application dedupe, evidence thresholds, size limits and reporter-proof rate limits exist; IP/device reputation and DDoS controls belong at the production gateway. |

## Manual visible acceptance register

| ID | Status | Visible requirement |
|---|---|---|
| MAN-001 | MANUAL | Dashboard renders without blank/frozen state or visible script failure. |
| MAN-002 | MANUAL | Desktop and narrow layouts remain readable and controls do not overlap. |
| MAN-003 | MANUAL | All five fixture providers complete visible Quick scans. |
| MAN-004 | MANUAL | Full and Spam/Junk scans show bounded progress and Stop remains responsive. |
| MAN-005 | MANUAL | Safe audit clearly separates Report Scam, Move to Spam/Junk, Trust sender and unsubscribe. |
| MAN-006 | MANUAL | Report Scam confirmation explains privacy boundary, independent thresholds and optional sender block. |
| MAN-007 | MANUAL | After Report Scam, rescan shows matching campaign locally Confirmed Threat and no mailbox message is moved unless separately requested. |
| MAN-008 | MANUAL | Move to Spam/Junk affects exactly one fixture message, remains moved on rescan and remains visibly separate from shared reporting. |
| MAN-009 | MANUAL | Account switching does not cross-link results/actions. |
| MAN-010 | MANUAL | Controlled live iCloud scan remains responsive and credentials are not displayed. |
| MAN-011 | MANUAL | Normal dashboard actions continue without visible CSRF/session errors; refreshing creates a new valid local session; a stale tab after process restart shows a clear reload requirement rather than silently performing an action or hanging on scan startup. |
| MAN-012 | MANUAL | Rapid repeated clicks do not execute one successful message action twice; used action controls require a rescan. |
| MAN-013 | MANUAL | A controlled live iCloud Quick Scan shows a waiting update at least every 15 seconds, begins displaying results as bounded batches complete, and never remains indefinitely on Scanning; a genuinely stalled provider returns a specific timeout error and releases the scan controls. |
| MAN-014 | MANUAL | A controlled live iCloud scan extracts readable text from ordinary short messages without uniformly marking them partial or showing the former `Readable text was bounded to 24576 bytes` note; HTML-only/multipart messages expose applicable content/link evidence while genuinely oversized parts remain visibly bounded. |
| MAN-015 | MANUAL | A persisted sender/domain block is visible on rescan, can be removed from the card, and the personal block evidence disappears after another rescan; locally reported campaigns display a clear protected state and do not re-offer conflicting Safe/Trust decisions. |
| MAN-016 | LIVE-PASS | Guided Gmail opens Google consent, returns through the one-time loopback callback, adds the Gmail account, completes Quick Scan without exposing OAuth credentials, Disconnect succeeds, the same account reconnects, and Quick Scan succeeds after reconnect. |
| MAN-017 | MANUAL | Guided Outlook opens Microsoft consent as a public desktop client, returns through the one-time localhost PKCE callback, adds the Outlook account, completes Quick Scan, Disconnect removes the local protected credential, and the same account reconnects/Quick-Scans without policy identity changing after refresh-token rotation. |
| MAN-018 | MANUAL | The Personal Policy Management panel follows the selected account, search/filter is readable, single and bulk revoke visibly refresh the counts/list, policy-only export downloads, merge/replace import updates only that account, and Reset removes all six policy classes only after the exact confirmation phrase. |

## Register maintenance rule

Every fixed defect receives a `REG-*` entry and automated protection. Pre-existing findings and dependency baselines remain recorded. Resolved gaps remain in the historical register. Deployment limitations remain `GAP-*` until the actual environment proves them. Do not delete history to make the register appear green.
